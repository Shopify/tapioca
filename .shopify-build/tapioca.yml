containers:
  default:
    docker: "circleci/ruby:2.5.1"

steps:
- label: ":ruby: Specs"
  dependencies:
  - "bundler"
  timeout: "5m"
  run:
  - "bin/test"
- label: "Rubocop"
  dependencies:
  - "bundler"
  timeout: "5m"
  run: "bin/style"
